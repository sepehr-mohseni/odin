# This is an example auth_secrets.yaml file
# IMPORTANT: Do not commit your actual secrets file to version control!
# Copy this file to auth_secrets.yaml and replace the example secret with your own.

# JWT Secret for token signing and validation
# Use a strong, randomly generated value in production
jwtSecret: "REPLACE_THIS_WITH_A_SECURE_RANDOM_STRING_AT_LEAST_32_CHARACTERS_LONG"
